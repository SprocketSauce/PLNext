PROGRAM
	DECL :: testprogram
		CONST pi IS 3,
		a IS 1;

		VAR b : baa,
		c : caa;

		TYPE testtype => atype;

		PROC myproc(a;b;c)

		FUNC myfunc(d)

	DECLARATION END

	IMPL :: testimplementation
		FUNC func;
			PROC noimplementation;
				VAR a : aa, b : bb;
			;

			WHILE 2 * 3 + 4 / 5 - 6
				whileboi SET 9000
			ELIHW
		;
		
		START
			settyboi SET 3 + 9
		STOP
	.

TERMINATE
